task pylint(type:Exec) {
  //println 'Setting up dependencies'
  executable 'sh'
  args '-c', 'pylint src/main.py'
  standardOutput = new ByteArrayOutputStream()
  ext.output = { return standardOutput.toString() }
}
